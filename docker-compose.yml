#-----------LOCAL COMPOSE-----------
services:
  bot:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./daily.log:/app/daily.log

volumes:
  logs:

#-----------REMOTE COMPOSE-----------
#services:
#  telegram-bot:
#    image: hub_username/hub_image:latest
#    container_name: telegram-bot
#    restart: always
#    volumes:
#      - .env:/app/.env
#      - ./daily.log:/app/daily.log
#    ports:
#      - "8080:8080"